<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib">

    <Style x:Key="AppScrollPageButton" TargetType="RepeatButton">
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="IsTabStop" Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton"><Border Background="Transparent"/></ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="AppScrollThumb" TargetType="Thumb">
        <Setter Property="MinHeight" Value="28"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Grid Background="Transparent">
                        <Border x:Name="Grip" Width="8" HorizontalAlignment="Center" Margin="0,2" CornerRadius="3" Background="{DynamicResource PageScrollThumbBrush}"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <DataTrigger Binding="{Binding Orientation, RelativeSource={RelativeSource AncestorType=ScrollBar}}" Value="Horizontal">
                            <Setter TargetName="Grip" Property="Width" Value="Auto"/>
                            <Setter TargetName="Grip" Property="Height" Value="8"/>
                            <Setter TargetName="Grip" Property="HorizontalAlignment" Value="Stretch"/>
                            <Setter TargetName="Grip" Property="VerticalAlignment" Value="Center"/>
                            <Setter TargetName="Grip" Property="Margin" Value="2,0"/>
                        </DataTrigger>
                        <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Grip" Property="Background" Value="{DynamicResource PageScrollThumbHoverBrush}"/></Trigger>
                        <Trigger Property="IsDragging" Value="True"><Setter TargetName="Grip" Property="Background" Value="{DynamicResource PageScrollThumbPressedBrush}"/></Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <DataTrigger Binding="{Binding Orientation, RelativeSource={RelativeSource AncestorType=ScrollBar}}" Value="Horizontal">
                <Setter Property="MinHeight" Value="0"/><Setter Property="MinWidth" Value="28"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="AppScrollBarStyle" TargetType="ScrollBar">
        <Style.Resources>
            <!-- Track uses half these metrics as the minimum thumb length in either direction. -->
            <sys:Double x:Key="{x:Static SystemParameters.VerticalScrollBarButtonHeightKey}">96</sys:Double>
            <sys:Double x:Key="{x:Static SystemParameters.HorizontalScrollBarButtonWidthKey}">96</sys:Double>
        </Style.Resources>
        <Setter Property="Width" Value="12"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                    <Grid Background="{TemplateBinding Background}">
                        <Track x:Name="PART_Track" Orientation="{TemplateBinding Orientation}" IsDirectionReversed="True">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton x:Name="PageBefore" Command="ScrollBar.PageUpCommand" Style="{StaticResource AppScrollPageButton}"/>
                            </Track.DecreaseRepeatButton>
                            <Track.Thumb><Thumb Style="{StaticResource AppScrollThumb}"/></Track.Thumb>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton x:Name="PageAfter" Command="ScrollBar.PageDownCommand" Style="{StaticResource AppScrollPageButton}"/>
                            </Track.IncreaseRepeatButton>
                        </Track>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Orientation" Value="Horizontal">
                            <Setter TargetName="PART_Track" Property="IsDirectionReversed" Value="False"/>
                            <Setter TargetName="PageBefore" Property="Command" Value="ScrollBar.PageLeftCommand"/>
                            <Setter TargetName="PageAfter" Property="Command" Value="ScrollBar.PageRightCommand"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False"><Setter TargetName="PART_Track" Property="Opacity" Value="0.4"/></Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Width" Value="Auto"/><Setter Property="Height" Value="12"/>
            </Trigger>
        </Style.Triggers>
    </Style>
    <!-- Includes scroll bars inside TextBox, DataGrid, lists, dropdowns and page viewers. -->
    <Style TargetType="ScrollBar" BasedOn="{StaticResource AppScrollBarStyle}"/>
</ResourceDictionary>
